# Support

For local development:

1. Run the extension with `F5`.
2. Open `ThreadVault: Open Logs`.
3. Re-run `ThreadVault: Rescan Local History` and collect the visible output.

For public release, replace this file with links to your repository issues page or support site.
